<Window x:Class="Tasky.Views.Utils.MessageWarning"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tasky.Views.Utils"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="MessageWarning" Height="150" Width="250">
    <Grid Background="#141F4A" x:Name="Grids">
        <TextBlock Text="ATENÇÃO!" VerticalAlignment="Top" Margin="8,6,0,0"
            FontWeight="Bold" FontSize="12" Foreground="White"/>

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
            x:Name="MessageToUser" FontWeight="Bold" Foreground="White"/>
    </Grid>

    <!--    <Grid Background="Gray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="137"/>
            <ColumnDefinition Width="225"/>
            <ColumnDefinition Width="137"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Grid Grid.RowSpan="2" Grid.Column="1">
            <Rectangle Fill="SlateGray" Height="100"/>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
            x:Name="MessageToUser" FontWeight="Bold"/>

            <TextBlock Text="ATENÇÃO!"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,55,0,0"
            FontWeight="Bold" FontSize="12"/>
        </Grid>
    </Grid>-->
</Window>
